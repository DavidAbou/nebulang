exp strLen(str: String): Int
{
    set len: Int = 0;

    iter (c: Char in str) {
        len = len + 1;
    }
    emit len;
}

exp seqLen(str: Seq[Int]): Int
{
    set len: Int = 0;

    iter (c: Int in str) {
        len = len + 1;
    }
    emit len;
}
