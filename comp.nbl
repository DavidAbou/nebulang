exp min(x y) {
    if (x < y) {
        emit x;
    } otherwise {
        emit y;
    }
}

exp max(x y) {
    if (x > y) {
        emit x;
    } otherwise {
        emit y;
    }
}

atom imported = "I'm imported!";
